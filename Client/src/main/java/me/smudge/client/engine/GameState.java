package me.smudge.client.engine;

/**
 * Represents the game state
 */
public enum GameState {

    // Default when running
    Running,

    // Stopped safely
    Stopped,

    // Error occurred
    Crashed
}
